name = "drinking-game-api"
main = "index.js"
compatibility_date = "2024-01-01"

# 绑定自定义域名（国内可访问，绕过 workers.dev 被墙）
[[routes]]
pattern = "api.banzhang.icu"
custom_domain = true

[vars]
ALLOWED_ORIGIN = "https://guonian.banzhang.icu"
